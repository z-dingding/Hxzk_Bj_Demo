<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hxzk_bj_demo">


    <!-- ======================授权访问网络（HttpUtil）========================== -->
    <!-- 允许一个程序访问精良位置(如GPS)  -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--访问CellID或WiFi,只要当前设备可以接收到基站的服务信号，便可获得位置信息。-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- 访问电话状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- ======================授权访问网络（MyWebView）========================== -->
    <!-- 允许程序打开网络套接字 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 允许程序访问有关GSM网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 允许程序访问Wi-Fi网络状态信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- ======================拍照用到的========================== -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- ======================录音用到的========================== -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 在SD卡中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 向SD卡写入数据权限 -->
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 横竖屏切换的权限-->
    <uses-permission android:name="android.permission.CHANGE_CONFIGURATION" />

    <application
        android:name=".common.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.CustomTheme.Day"
        android:resizeableActivity="true"
         tools:replace="android:theme,name"
         >
        <!--targetSdkVersion==24（Android 7.0）以上Google默认支持了分屏模式-->
        <!--适配19:9全面屏黑边问题：https://blog.csdn.net/darkeet/article/details/80049913-->
        <!--https://blog.csdn.net/weelyy/article/details/79284332
         提高App所支持的最大屏幕纵横比（设置android.max_aspect比例值）-->
        <meta-data
            android:name="android.max_aspect"
            android:value="2.2"/>

        <!--AndroidAutoSize的适配需要先填写效果图的宽高单位dp-->
        <meta-data
            android:name="design_width_in_dp"
            android:value="360"/>
        <meta-data
            android:name="design_height_in_dp"
            android:value="640"/>

        <!--android:theme="@style/TranslucentTheme">-->
        <activity android:name=".ui.activity.WelcomeActivity"
            android:theme="@style/welcometheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--  登录界面  -->
        <activity android:name=".ui.activity.LoginActivity"
            android:configChanges="locale"/>
        <!--  注册界面  -->
        <activity android:name=".ui.activity.RegisterActivity"
            android:configChanges="locale"/>
        <!--  主页面  -->
        <activity android:name=".ui.activity.MainActivity"
            android:configChanges="locale"
                 ></activity>
        <!-- 收藏界面 -->
        <activity android:name=".ui.activity.CollectionActivity"
            android:configChanges="locale"/>
        <!-- 笔记本 -->
        <activity android:name=".mvp.view.NoteBookActivity"
            android:configChanges="locale"/>
        <!-- 二维码界面     android:theme="@style/TranslucentTheme"-->
        <activity android:name=".utils.zxing.activity.CaptureActivity"
            android:configChanges="locale"
            android:screenOrientation="portrait"
            />
        <!-- 其他方式登录 -->
        <activity android:name=".ui.activity.OntherWayLoginActivity"
            android:configChanges="locale"/>
        <!-- 首页搜索界面-->
        <activity android:name=".ui.activity.HomeSearchActivity"/>
        <!--图案解锁界面-->
        <activity android:name=".ui.activity.PatternLockActivity"/>
    </application>



</manifest>